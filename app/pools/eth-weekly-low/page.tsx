"use client";
import { useState } from "react";
import Link from "next/link";
import { AssetBadge, Header, Mountain } from "../../components";
import { ethRound } from "../../lib/mock-data";

export default function Prediction() { const [value, setValue] = useState("2085.00"); const [confirmed, setConfirmed] = useState(false); return <main className="paper prediction-page"><Header /><section className="prediction-layout"><aside className="round-brief"><Link href="/pools">← &nbsp; Back to Pools</Link><div className="brief-title"><AssetBadge asset="ETH" /><div><h2>ETH<br /><span>Weekly Low</span></h2><p>{ethRound.period}</p></div></div><strong>{ethRound.current}</strong><em>+2.4% (24h)</em><Mountain /><p className="question">How low will ETH go<br />this week?</p><div className="source-card"><small>Official price source</small><b>◇ &nbsp; Binance Futures</b><span>ETHUSDT Mark Price</span></div></aside><section className="entry-panel"><h1>{confirmed ? "Prediction reserved." : "Your Prediction"}</h1><label>Your Prediction (USD)<div className="input-wrap"><span>$</span><input value={value} onChange={(event) => setValue(event.target.value)} inputMode="decimal" /><b>⌃<br />⌄</b></div></label><div className="entry-details"><span>Entry Fee <b>1 USDC</b></span><span>Round Ends <b>Sun, Oct 12, 23:59 UTC</b></span></div><p className="ticket-preview"><AssetBadge asset="ETH" /><span>EXTREMA Ticket<br /><b>ETH · Weekly Low</b><br />Round #184 · 1 USDC</span></p><button className="button dark full" onClick={() => setConfirmed(true)}>{confirmed ? "Ticket ready · View live pool" : "Confirm & Mint Ticket"} <b>→</b></button><p className="quiet">ⓘ &nbsp; Each prediction is unique. Same price cannot be taken twice.</p></section></section></main>; }
